# custom_migration

This application is for creating custom migraiotn in the django.

Migration is mianly for keeping data model up-to-date.All this is managed by table named "django_migrations" which gets
created when we first apply the migrations.
